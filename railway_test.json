{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x railway_build_test.sh && ./railway_build_test.sh"
  },
  "deploy": {
    "startCommand": "python test_app.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
